import type { NextConfig } from 'next';

const nextConfig: NextConfig = {
  // FreeBusy-only calendar sync, SMS-first delivery — no config knobs needed
  // here yet. Revisit when image domains (avatars, plan covers) are added.
};

export default nextConfig;
